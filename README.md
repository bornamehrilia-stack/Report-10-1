# Report-10-1



---

# 📋 گزارش پروژه: نمایش حرف «a» روی ماتریس LED 8×8 با کتابخانه MaxMatrix

## ۱. مقدمه  
در این پروژه, یک **ماتریس LED 8×8** (با کنترلر MAX7219) به آردوینو متصل شده و با استفاده از کتابخانه `MaxMatrix.h`, یک شکل به دلخواه روی ماتریس نمایش داده می‌شود. 

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- ماتریس LED 8×8 با کنترلر MAX7219  
- برد برد و سیم‌های جامپر  
- منبع تغذیه (معمولاً 5V از آردوینو کافی است)



```

## ۵. کد برنامه (اسکچ)

```cpp
#include <MaxMatrix.h>   // اضافه کردن کتابخانه

int DIN = 7;      // Data In
int CLK = 6;      // Clock
int CS = 5;       // Chip Select
int maxInUse = 1; // تعداد ماژول‌ها

byte buffer[20];        // بافر برای ذخیره داده‌ها (اختیاری در این کد)
char text[] = "a";     // متن برای نمایش (فقط حرف 'a')

// ایجاد شیء ماتریس
MaxMatrix m(DIN, CS, CLK, maxInUse);

void setup() {
  m.init();           // راه‌اندازی ماژول
  m.setIntensity(8);  // تنظیم شدت نور (0–15)
}

void loop() {
 
  m.setDot(0, 7, true);  
  delay(1000);
  m.setDot(0, 7, true);   
  delay(1000);
  m.setDot(7, 0, true);
  delay(1000);
  m.setColumn(3, B11110000);
  delay(1000);
  m.setColumn(4, B00001111);  
  delay(1000);
  m.clear();
  delay(1000);
}
```


## ۷. نتایج  
- ماتریس LED به درستی الگوهای تعریف‌شده را نمایش می‌دهد.  


---

